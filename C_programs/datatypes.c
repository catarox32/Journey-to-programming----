#include <stdio.h>

int main()
{
    //// integer types

    // int foo = 2; // 32 or 64 bit depending on the system
    // short int bar = 30; // 16 bit
    // long int baz = 30; // 32 bit
    // long long int bat = 40; // 64 bit

    // printf("signed int types %d and %d and %d and %d\n", foo * 3, bar / 2, baz * 3, bat % 10);

    // unsigned int ufoo = 300;
    // unsigned short int ubar = 250;
    // unsigned long int ubaz = 5348;
    // unsigned long long int ubat = 501231302;

    // printf("unsigned int types %d and %d and %d and %d\n", ufoo * 3, ubar / 2, ubaz * 3, ubat % 10);

    //// char and string (char[]) types
    // char foo = 'A'; // -128 to 255
    //// 8 bit
    // signed char bar = 'B';   // -128 to 127
    // unsigned char baz = 'C'; // 0 to 255

    // printf("ascii value for %c is %d\n", foo, (int)foo);

    //// inplicit typecasting
    // short int foo = 20;
    // long int bar = 30;
    // long int baz = foo + bar;

    // printf("%d", baz);

    //// floating point types
    // float foo = 24.0;
    // double bar = 64.0;
    // long double baz = 69412.0;


    return 0;
}